// Now / Today is no longer a separate experience — the canonical Daily Home
// already leads with Now, Today and Needs attention. Kept as an alias so old
// navigation entries and deep links keep working.
export { default } from "@/pages/DashboardHome";
