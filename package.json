{
  "name": "vite_react_shadcn_ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/patch-dexie.mjs && vite build",
    "build:dev": "node scripts/patch-dexie.mjs && vite build --mode development",
    "pages:build": "npm run build",
    "validate": "npm run lint && npx tsc --noEmit && npm test && npm run